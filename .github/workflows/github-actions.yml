name: GitHub Actions
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  NEXT_PUBLIC_JWT_SECRET: 'Kazan2022!'
  NEXT_PUBLIC_SECRET: 'secret'
  NEXT_PUBLIC_IMAGES_DOMAIN: ${{ NEXT_PUBLIC_IMAGES_DOMAIN }}
  NEXT_PUBLIC_BOT_TOKEN: '5396704177:AAFhWyE9yyM7iTfQ-AJQuSpHTEEyaQm6tfk'
  NEXT_PUBLIC_APP_URL: 'https://innoads.ru'
  NEXT_PUBLIC_API_URL: 'https://chamala.tatar'
  NEXT_PUBLIC_NODE_ENV: 'development'
  NEXT_PUBLIC_CHAT_NAME: '@innoads'
  EXT_PUBLIC_TEST_TOKEN: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6NzEyMzM0ODAsInVzZXJuYW1lIjoibWFyYXRmYWl6ZXIiLCJpYXQiOjE2NzMyODIxODcsImV4cCI6MTcwNDgxODE4N30.W2-jzWy3-3On2JrKm-jNoW2kvvw3I_t9tCY5nZuoLkk'

jobs:
  init:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 18.x ]
    steps:
      - uses: actions/checkout@v3
      - name: Staring Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: install modules
        run: yarn
      - name: build project
        run: yarn build
